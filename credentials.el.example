;; Jabbber
(setq jabber-account-list '(
  ("jabber_account"
    (:password . "jabber_password")
    (:network-server . "talk.google.com")
    (:port . 443)
    (:connection-type . ssl))))

(setq jabber-account-username "jabber_account")
(setq jabber-window1-username "account1")
(setq jabber-window2-username "account2")
(setq jabber-window3-username "account3")

;; Mu4e / Smtp
(setq
 user-mail-address "from address"
 user-full-name  "from name"
 message-signature
 (concat
  "name\n"
  "title\n"
  "url\n"))

;; SQL connections
(setq sql-connection-alist
      '((mysql-example-connection
         (sql-product 'mysql)
         (sql-server "localhost")
         (sql-user "user")
         (sql-password "password")
         (sql-database "database")
         (sql-port 3306))

        (postgres-example-connection
         (sql-product 'postgres)
         (sql-server "localhost")
         (sql-user "user")
         (sql-password "password")
         (sql-database "database")
         (sql-port 5432))))
